spring:
  application:
    name: Userservice

  datasource:
    url: jdbc:mysql://localhost:3306/user_fitness_db
    username: root
    password: Aman143#

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eurwka/