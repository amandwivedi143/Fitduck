spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/fitness_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true"

    username: "root"
    password: "Aman143#"
    #driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: ${PORT:8081}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

