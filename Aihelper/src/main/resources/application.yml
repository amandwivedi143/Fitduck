spring:
  application:
    name: aihelper-service
  config:
    import: optional:configserver:http://localhost:8888
  data:
    mongodb:
      uri: mongodb://localhost:27017/fitness_mealplan
      database: fitness_mealplan
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

rabbitmq:
  exchange:
    name: mealplan.exchange
  queue:
    name: mealplan.queue
  routing:
    key: mealplan.routing
  workout:
    exchange:
      name: workoutplan.exchange
    queue:
      name: workoutplan.queue
    routing:
      key: workoutplan.routing

groq:
  api:
    url: "https://api.groq.com/openai/v1/chat/completions"
    key: ${GROQ_API_KEY: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}
